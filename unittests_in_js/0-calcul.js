// Create a function named calculateNumber
// Accepts two arguments, rounds, and returns the sum

function calculateNumber(a, b) {
    return Math.round(a) + Math.round(b);
}

module.exports = calculateNumber;
